complete \
  --command 'gi' \
  --description '.gitignore generator using gitignore.io' \
  --arguments "(gi list | string split ',')" \
  --no-files

complete \
  --command 'gi' \
  --arguments 'list' \
  --description 'List all templates' \
  --no-files
